{
  "name": "growth-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --no-warnings server/index.js",
    "test": "node --no-warnings --test"
  },
  "engines": {
    "node": ">=24"
  }
}

